//2
//3
//4
//

begin
    int x = 9;
    int y = 0-9;

    println(((x+y)==0) ? (y > 0) ? 1 : 2 : 3);
    println((x < 0) ? (y > 0) ? 1 : 2 : 3);
    println((x < 0) ? (y > 0) ? 1 : 2 : (x == y) ? 3 : 4);
end